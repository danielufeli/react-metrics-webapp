import React from 'react';
import ReactDOM from 'react-dom';
import './index.scss';

ReactDOM.render(
  <React.StrictMode>
    <div>
      <h1>Test</h1>
    </div>
  </React.StrictMode>,
  document.getElementById('root'),
);
